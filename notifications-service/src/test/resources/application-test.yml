spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:notificationsdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    enabled: false
  mail:
    host: localhost
    port: 2525
    username: test
    password: test
  cloud:
    openfeign:
      circuitbreaker:
        enabled: false
logging.level.org.springframework: WARN
