{{- with .Values.config.configMap }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "transfer.fullname" $ }}-config
  labels:
    {{- include "transfer.labels" $ | nindent 4 }}
data:
{{- range $key, $value := . }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
{{- end }}
