{{- with .Values.config.configMap }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "gateway.fullname" $ }}-config
  labels:
    {{- include "gateway.labels" $ | nindent 4 }}
data:
{{- range $key, $value := . }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
{{- end }}
