{{- with .Values.config.secret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "auth-server.fullname" $ }}-secret
  labels:
    {{- include "auth-server.labels" $ | nindent 4 }}
stringData:
{{- range $key, $value := . }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
{{- end }}
