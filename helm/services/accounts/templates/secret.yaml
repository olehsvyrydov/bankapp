{{- with .Values.config.secret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "accounts.fullname" $ }}-secret
  labels:
    {{- include "accounts.labels" $ | nindent 4 }}
stringData:
{{- range $key, $value := . }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
{{- end }}
