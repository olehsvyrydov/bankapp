{{- if .Values.database.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "exchange-generator.fullname" . }}-db
  labels:
    {{- include "exchange-generator.labels" . | nindent 4 }}
  {{- with .Values.database.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: ClusterIP
  ports:
    - name: postgres
      port: {{ .Values.database.service.port }}
      targetPort: postgres
  selector:
    app.kubernetes.io/name: {{ include "exchange-generator.name" . }}-db
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}
