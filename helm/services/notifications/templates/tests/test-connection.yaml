{{- if (default true .Values.tests.enabled) }}
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "notifications.fullname" . }}-test"
  labels:
    {{- include "notifications.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
    - name: curl
      image: {{ .Values.tests.image | default "curlimages/curl:8.10.1" }}
      args: ['-f', 'http://{{ include "notifications.fullname" . }}:{{ .Values.service.port }}{{ .Values.tests.path | default "/actuator/health" }}']
{{- end }}
