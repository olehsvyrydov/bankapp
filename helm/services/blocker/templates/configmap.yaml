{{- with .Values.config.configMap }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "blocker.fullname" $ }}-config
  labels:
    {{- include "blocker.labels" $ | nindent 4 }}
data:
{{- range $key, $value := . }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
{{- end }}
